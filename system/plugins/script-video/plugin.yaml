name: Video từ kịch bản (ảnh AI + TTS)
slug: script-video
version: 1.2.0
description: Render mp4 đầy đủ - ảnh AI ChatGPT từng cảnh + Edge-TTS + ffmpeg; Pixelle tuỳ chọn.
author: Javis (bundled)
enabled: true
min_mode: safe
tools:
  - javis_render_script_video
  - javis_pixelle_generate
hooks: []
