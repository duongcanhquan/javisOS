name: Video từ kịch bản (ffmpeg + Pixelle)
slug: script-video
version: 1.1.0
description: Render mp4 từ kịch bản - native Edge-TTS+ffmpeg và/hoặc Pixelle API (fallback tự động).
author: Javis (bundled)
enabled: true
min_mode: safe
tools:
  - javis_render_script_video
  - javis_pixelle_generate
hooks: []
